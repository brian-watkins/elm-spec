{
  "name": "elm-spec",
  "version": "1.0.0",
  "description": "Describe the Behavior of Elm Applications",
  "scripts": {
    "start": "cd runner/tests/sample && ../../elm-spec-runner/bin/run",
    "test": "run-s test:elm test:elm:browser test:runner test:spec-runner build:karma test:karma",
    "test:elm": "cd tests && ELM_SPEC_CONTEXT=jsdom mocha '*Test.js'",
    "test:elm:browser": "cd tests && ELM_SPEC_CONTEXT=browser mocha --file ./helpers/browserRunnerSetup.js --timeout 5000 '*Test.js'",
    "test:runner": "mocha './runner/tests/*Test.js'",
    "test:spec-runner": "npm test --prefix runner/elm-spec-runner",
    "build:karma": "npm run build --prefix runner/karma-elm-spec-framework",
    "test:karma": "npm test --prefix runner/karma-elm-spec-framework"
  },
  "author": "Brian Watkins",
  "license": "MIT",
  "dependencies": {
    "elm": "^0.19.1-3"
  },
  "devDependencies": {
    "browserify": "^16.5.0",
    "chai": "^4.2.0",
    "elm-spec-core": "file:runner/elm-spec-core",
    "mocha": "^6.2.0",
    "npm-run-all": "^4.1.5",
    "oclif": "^1.13.6",
    "puppeteer": "^2.0.0"
  }
}
